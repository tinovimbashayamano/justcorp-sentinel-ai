const RISKS=["critical","high","medium","low"];
export default function RiskDistributionBars({summary,onSelect}){const d=Math.max(1,summary.total);return <section className="risk-distribution-panel"><div className="transaction-panel-header"><div><p className="transaction-eyebrow">Risk distribution</p><h2>Transaction risk profile</h2></div></div><div className="risk-bars">{RISKS.map(r=>{const c=summary[r]||0;return <button type="button" className="risk-bar-row" key={r} onClick={()=>onSelect(r)}><span>{r}</span><div className="risk-bar-track"><div className={`risk-bar-fill risk-bar-fill--${r}`} style={{width:`${(c/d)*100}%`}}/></div><strong>{c}</strong></button>})}</div></section>}
